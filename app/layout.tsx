import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Akari Social | Find Your Bottleneck",
  description: "Take this quick diagnostic quiz to discover what's holding you back and get a personalized plan to break through.",
  icons: {
    icon: "/wizard.png",
    apple: "/wizard.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
